meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
run:
  persistenceFolder: /data
  containerPort: 80
  scriptName: manage.py
  args: runserver 0.0.0.0:80